"""
Artifacts module.

Owns the database tables that back the Plexichat "Artifacts" feature:
persistent, first-class records for voice calls, whiteboards, uploads,
files, transcripts, and future artifact types. This package currently
exposes only the database schema; models and manager logic are added in
later groups.
"""
