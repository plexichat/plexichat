"""
Migration files for database schema changes.

Each migration file should be named with a zero-padded version number followed by
a descriptive name, e.g., 001_initial_schema.py, 002_add_users_table.py

Migration files must contain an up(db) function and optionally a down(db) function.
"""
