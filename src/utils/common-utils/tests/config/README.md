# Config Tests

Unit tests for the ConfigLoader utility.

## Test Coverage

- `test_config.py` - Tests for YAML/JSON config loading, default creation, malformed file handling, and save operations

## Key Test Cases

- Creating default config files (YAML and JSON)
- Handling malformed config files (crash vs ignore modes)
- Setting and saving configuration values
- Config file format detection
