database:
  path: temp_test_security/test_security.db
  type: sqlite
media:
  local_path: /tmp/media_test_y45b10e6/uploads_170639
  local_url: /media
  rate_limit:
    enabled: true
    max_total_size_per_day: 1073741824
    uploads_per_hour: 5
    uploads_per_minute: 1000
  signing_key: test-key
  storage_backend: local
